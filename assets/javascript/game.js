/* Javascript */


/*
countGuess(GuessArray, UserGuess)
{
    UserGuess.count++;
    if (UserGuess.count > 8) 
    {                              // Out of guesses
        return -1;
    }

    // Add the guess to the array of guesses to keep track
    GuessArray = GuesArray + UserGuess; 
}
*/